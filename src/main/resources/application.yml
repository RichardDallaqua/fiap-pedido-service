application:
  title: "Aplicação desenvolvida por: AmandaPoreto, AndréMelchior e RichardDallaqua"

spring:
  data:
    mongodb:
      uri: ${MONGO_URL:mongodb://root:root@127.0.0.1:27017/db?authSource=admin}
  rabbitmq:
      host: ${RABBIT_HOST:localhost}
      port: 5672
      username: ${RABBIT_USER:guest}
      password: ${RABBIT_PASS:guest}
queue:
  pagamento: pagamento

server:
  port: 5000
  servlet:
    context-path: /pedido-ms

host:
  payment: http://localhost:8080/fiap-payments/api/v1/payments
